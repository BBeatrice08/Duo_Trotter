{% extends "layout.html.twig" %}


{% block title %}Articles List{% endblock %}

{% block content %}
    <h1>Articles list</h1>
    {% for article in articles %}
        <h2>{{ article.title }}</h2>
        <p>{{ article.date }}</p>
        <p>{{ article.content }}</p>
    {% endfor %}
{% endblock %}
